<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- saveBlob (utils/native.js) writes shared files to Directory.Cache only;
         no other directory should be exposable through the provider. -->
    <cache-path name="my_cache_images" path="." />
</paths>
