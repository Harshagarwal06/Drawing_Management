# DrawVault — agent instructions

See **CLAUDE.md** in this directory — the single source of truth for project rules, commands,
and gotchas. Deep reference (API endpoints, DB schema, env vars): `docs/ARCHITECTURE.md`.
